class TextFieldValidation{
  late bool isValid=true;
  late String? message;

  TextFieldValidation({required this.isValid, this.message});
}

class DropdownValidation{
  late bool isValid=true;
  late String? message;

  DropdownValidation({required this.isValid, this.message});
}